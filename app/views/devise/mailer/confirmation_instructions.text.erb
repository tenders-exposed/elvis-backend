Welcome <%= @email %>!

You can confirm your account email through the link below:

<%= confirmation_url(@resource, confirmation_token: @token).gsub('users/confirmation?confirmation_token', 'account/confirm?t').gsub('api/v1/', '') %>

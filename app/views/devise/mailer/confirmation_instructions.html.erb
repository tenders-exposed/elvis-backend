<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token).gsub('users/confirmation?confirmation_token', 'account/confirm?t').gsub('api/v1/', '') %></p>
